import 'package:flutter/material.dart';

class PropertySummary extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Property Summary'),
      ),
      body: Container(
        child: Text(
            'This is still to be developed. Will show property history and ROI etc.'),
      ),
    );
  }
}
